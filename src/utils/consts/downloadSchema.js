const NAME = "name";
const DEVICE = "device";
const PATH = "path";
const STATUS = "status";
const AVAILABLE = "available";

const DOWNLOADABLE_KEYS = {
  NAME,
  DEVICE,
  PATH,
  STATUS,
};

export { DOWNLOADABLE_KEYS, AVAILABLE };
